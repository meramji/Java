package Basics;

public class Division_of_datatypes {
    static void main() {
//        double x=5/2; if both divided number are int then output will also come in int.double can store decimal values but two int values on dividing gives int value only.

//        double x=5.0/2; if one is int and second is decimal it will give decimal in output.

        double x=5.0/2.0;// if both are decimal values then it will give decimal value.

        System.out.println(x);
    }
}
