package Basics;

public class Arthimeticoperations {
    static void main() {
        double x=45;
        double y=34;
        //double is used to get value in decimals if we use int it will give value only in integer and it will hide the decimals places
        System.out.println(x+y);
        System.out.println(x-y);
        System.out.println(x*y);
        System.out.println(x/y);//double is prefer over float as it stores more values.
    }
}
